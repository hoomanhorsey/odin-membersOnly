




Things to do:

- can see date and author of posts without being a member. Need to restrict to members only.

Delete does not delete post.



- can access membership screen if not logged in, should check for authorisation of route before allowing access, otherwise it's unauthroised.


- logout route, including actually log out

- membership join route / query

- admin status?

- Authorisation, think about route checking for queries, think about authorisation to determine info being displayed in views





Issues

- signUp
    - duplicate data, like same email addresses, cause an error.


CREATE TABLE users ( 
    id INTEGER PRIMARY KEY,
    firstName TEXT, 
    lastName TEXT,
    email TEXT UNIQUE NOT NULL,  
    password TEXT,
    membershipStatus BOOLEAN,
    admin BOOLEAN
    );

CREATE TABLE messages (
    id INTEGER PRIMARY KEY,
    user_id INTEGER NOT NULL,
    messagetitle TEXT,
    messagetext TEXT NOT NULL,
    timeCreated TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_ID) REFERENCES users(id)
    );



Views 
- sign up page with sign up form
    - sanitize and validate form fields
    - bcrypt 
    - password confirmation, custom validator

- club joining (with secret password)
    - using secret password updates membership status

- login page with login form

- Create new message page

- home page
    - display all Messages
    - but author and date only visible to club membership
    - delete msg button only visible to admin



app.js / server.js  <- your main Express app
├─ routes/          <- route definitions
│   ├─ authRoutes.js
│   ├─ userRoutes.js
│   └─ messageRoutes.js
├─ controllers/     <- handle logic for each route
│   ├─ authController.js
│   ├─ userController.js
│   └─ messageController.js
├─ models/          <- optional, for database models or ORM
├─ queries/         <- optional, if using raw SQL queries
└─ middleware/      <- optional, authentication, logging, etc.